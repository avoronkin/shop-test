var shared = require('app/shared');
var cart = shared.cart;
var FullCartWidget = require('app/widgets/full-cart/Cart');

var template = require('./layout.html');

module.exports = {
    slug: 'cart',
    name: 'cart',
    layout: {
        options: {
            template: template,
            views: {
                '#full-cart': {
                    constructor: FullCartWidget,
                    options: {
                        collection: cart
                    }
                }
            }
        }
    }
};
